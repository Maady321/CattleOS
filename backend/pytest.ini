[pytest]
asyncio_mode = auto
testpaths = app/tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
env =
    ENVIRONMENT=test
    POSTGRES_DB=cattleos_test
    REDIS_DB=1
